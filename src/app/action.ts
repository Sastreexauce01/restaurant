"use server";
import { FormValues } from "@/components/Formulaire";


// enregistrer les abonnemenets

// enregistrer la demande demo

export async function addDemo(formsValues: FormValues) {
  try {
    if (!formsValues) return;

  
  } catch (error) {
    console.error("Erreur lors de l'enregistrement :", error);
  }
}

// enregistrer les abonnemenets

//envoi d'email de confirmation de notifaction
